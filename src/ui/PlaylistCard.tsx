import { Text, View } from "native-base";
import * as React from "react";

interface Props {}

export const PlaylistCard: React.FC<Props> = () => {
  return (
    <View>
      <Text>playlist card</Text>
    </View>
  );
};
